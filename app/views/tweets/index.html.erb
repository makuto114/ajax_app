<h1>Ajax_app</h1>
<%= link_to url: "/tweets", method: :post, id: "form" do |form| %>
  <%= form.text_field :content %>
  <%= form.submit '投稿する', id: "submit" %>
<% end %>

<% @tweets.each do |tweet| %>
  <div>
    <%= tweet.text %>
    <%= tweet.created_at %>
  </div>  
<% end %>
